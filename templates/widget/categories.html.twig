<div class="p-4">
    <h4 class="fst-italic">Catégories</h4>
    <ol class="list-unstyled mb-0">
        {% for cat in categories %}
            <li>
                <a class="text-decoration-none" href="{{ path('category_show',{slug: cat.slug}) }}">{{ cat.name }}</a>
            </li>
        {% endfor %}
    </ol>
</div>